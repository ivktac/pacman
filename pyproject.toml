[tool.pdm]
[tool.pdm.dev-dependencies]
dev = [
    "autopep8>=2.0.2",
]

[project]
name = ""
version = ""
description = ""
authors = [
    {name = "seshotake", email = "seshotake@gmail.com"},
]
dependencies = [
    "pygame>=2.3.0",
    "toml>=0.10.2",
]
requires-python = ">=3.10"
license = {text = "MIT"}
